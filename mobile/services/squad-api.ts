// Typed wrappers around echopay-cash backend (:8100) endpoints that proxy Squad.
// One function per backend route — auth/voice-signup, transfer/voice-initiate,
// dynamic-va/create, admin/state, etc.
export {};
